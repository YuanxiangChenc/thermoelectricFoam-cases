FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      TOP_patchGradDict;
}

functions
{
    TOP_patchGrad
    {
        type            surfaceFieldValue;
        libs            ("libfieldFunctionObjects.so");

        regionType      patch;
        name            (TOP);

        operation       none;
        fields          (snGrad(T) k);

        writeControl    timeStep;
        writeInterval   1;
    }
}

